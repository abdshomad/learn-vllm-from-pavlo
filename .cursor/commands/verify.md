Verify every feature of ray, vllm, grafana and promotheus integration using browser. Check which ports each services are served from the conf/env file. Check that the services using 2 GPU, can load TinyLLama and can serve LLM requests. 

Ray dashboard functions: Overview, Jobs, Serve, Cluster, Actors, Metrics, Logs

IMPORTANT: Visit every available links to capture their details and capture steps-by-steps in detail.